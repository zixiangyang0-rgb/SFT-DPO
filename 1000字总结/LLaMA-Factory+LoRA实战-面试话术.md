# LLaMA-Factory + LoRA 微调实战面试话术（1000字版）

> 适用岗位：NLP / 大模型算法工程师 / AI 应用工程师
> 核心策略：**项目叙述 + 数字落地 + 失败复盘 + 工具选型**

---

## 一、一句话开场（30秒定生死）

> "LLaMA-Factory + LoRA 是我做过最完整的端到端微调项目，从环境搭建、数据转换、WebUI 训练、对话测试到模型导出我全跑过一遍。最深的体会是三句话：**选对底座是起点、数据质量是命门、参数调优是分水岭**。我习惯用 Qwen2.5-3B-Instruct + LoRA + 50 条精标数据做情感分类，Loss 从 2.15 干到 1.8 才敢上线。"

一句话亮出"做过、复盘过、调通过"，比背概念强 10 倍。

---

## 二、为什么选 LLaMA-Factory（工具选型题）

| 场景 | 工具 |
|---|---|
| 学习原理 / 标准流程 | Hugging Face TRL（官方、文档全） |
| 个人单卡追求速度 | Unsloth（Triton 重写，快 2-5 倍） |
| **业务方零代码 / 快速验证** | **LLaMA-Factory（WebUI 点点鼠标）** |
| 大模型多卡分布式 | DeepSpeed ZeRO-3 |

**标准话术**：
> "LLaMA-Factory 的价值不是技术多牛，是把研究员的工作量降到了产品经理也能上手。**`llamafactory-cli webui` 一行命令拉起 Gradio 面板**，模型选择、数据集选择、超参配置全在浏览器点完，还能一键切换 Chat 页面对比微调前后效果。这种'低代码训练框架'在小公司或交付型项目里，是最快验证 PoC 的方式。"

---

## 三、数据工程（最常被追问）

**核心心法**：数据质量 > 数量，500-1000 条精标 > 5 万条粗标。

**标准流程**三步走：
1. **Label Studio 标注** → 导出原始 JSON（含 `id / data.text / annotations.result.value.choices`）
2. **写 Python 脚本转换** → Alpaca 格式 `{"instruction", "input", "output"}`
3. **注册到 `dataset_info.json`** → 框架的"数据户口本"

**我的踩坑代码**（面试可以背）：
```python
for item in raw_data:
    text = item['data']['text']
    label = item['annotations'][0]['result'][0]['value']['choices'][0]
    formatted_data.append({
        "instruction": "请分析以下评论情绪，只输出类别，不要解释。",
        "input": text,
        "output": label
    })
```

**进阶答案**：如果数据不够，我习惯让 AI 帮我"以一推十"——把现有样本丢给 GPT，说明训练目标，让它反推"还缺哪些表达方式"，补到满意为止。**LIMA 论文证明 1000 条精修数据能吊打 5 万条粗制数据**。

---

## 四、参数调优（必问，体现工程深度）

> "我做过四轮调参，每次踩不同的坑，正好把教科书上的概念都验证了一遍。"

| 轮次 | 配置 | Loss | 现象 |
|---|---|---|---|
| 第一轮 | LR=2e-4, Epoch=10, Rank=8 | 2.15 | 没学会，欠拟合 |
| 第二轮 | LR=2e-4, Epoch=10, Rank=8 | 1.3 | **Loss 降了但成复读机，过拟合** |
| 第三轮 | LR=5e-5, Epoch=3, Rank=16 | 降太慢 | 矫枉过正 |
| **第四轮** | **LR=1e-4, Epoch=8, Rank=16** | **1.8** | **平衡点** |

**面试金句**：
> "Loss 不是越低越好。**Loss=1.3 的模型比 1.8 的更差**，因为它把训练集背下来了，泛化能力归零。微调的判断标准是 'Loss 在下降 + 验证集不反弹 + 真实样本测试稳定'。"

**参数口诀**（务必背下来）：
- **Rank**：简单任务 8，复杂任务 64，**16 是性价比之选**
- **Alpha = 2 × Rank**（无脑设 32）
- **Learning Rate**：LoRA 用 **1e-4 ~ 2e-4**（比全量微调高一个数量级）
- **Epochs**：格式学习 8-15 轮、知识注入 3-5 轮、风格模仿 2-4 轮
- **Warmup Ratio = 0.1**：前 10% 步数慢慢升 LR，防止极端样本把参数带偏

---

## 五、Template（90% 的人会踩的坑）

> "Qwen 的 Template 是 `<|im_start|>user...<|im_end|>`，Llama 3 是 `<|begin_of_text|><|start_header_id|>user`。**你底座用的是 Qwen，Template 选 llama3，模型直接变复读机**。"

**原则：底座是谁，Template 就选谁**。

---

## 六、Base vs Instruct（区分度题）

- **Base 模型**：只会"文本补全"，听不懂"指令"。适合**从零微调专属领域**（医疗、法律）。
- **Instruct 模型**：经过 SFT + RLHF 的"成品人"，听得懂人话。**50 条精标数据就能调出好效果**。
- **选择建议**：小数据 + 通用任务 → Instruct；大数据 + 垂直领域 → Base。

---

## 七、训练后必做三件事

1. **看 Loss 曲线**（理想是从 2.x 平稳降到 1.0 以下）
2. **Chat 页面对比测试**（加载 vs 卸载 LoRA，同一 prompt 测两次）
3. **Export 合并权重**（LoRA 补丁 + 基座模型 = 可部署的完整模型，Max Shard Size 建议 2GB）

---

## 八、面试官追问时的救命模板

| 追问方向 | 一句话回应 |
|---|---|
| 为什么不直接全量微调 | 7B 模型全量微调要约 4 倍显存，单卡跑不动；还会灾难性遗忘 |
| 训练 Loss 不降 | 90% 是数据问题——格式错乱、字段没对齐、混进脏数据，先 print 几条看 |
| 怎么评估效果 | 主观靠 LLM-as-a-Judge 打分；客观看特定任务准确率；上线 A/B 看真实转化 |
| 数据多样性怎么保证 | 阴阳怪气有褒词贬用、冷嘲热讽、答非所问、只发"呵呵"等多种形态，不能只覆盖一种 |
| CoT 为什么有效 | 小模型（3B）逻辑链短，先拆解再下结论能把判断准确率拉高 10-20% |

---

**最后一句心法**：面试讲 LLaMA-Factory 项目，**不要只说"我跑通了"，要说"我跑挂了、又调活了"**。失败案例 + 调参表格 + 业务思考 = 高级感。
